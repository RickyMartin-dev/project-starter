# Context Artifacts

Use this directory for bounded, generated context that helps a specific phase of work. Every artifact should name its source, date, owner, and expiry or review condition.

Do not use this directory as an unbounded memory dump. Promote durable knowledge to `docs/architecture.md`, `docs/decisions/`, or `docs/lessons.md`, and delete stale generated context.
