# Implement prompt

Read `AGENTS.md` and the active plan before editing.

Implement only the requested scope. Reuse existing patterns, keep the diff reviewable, and update the plan when assumptions or steps change. Add or update tests at the behavior boundary. Run the narrowest useful check after each meaningful change, then run the full commands from `docs/project-commands.md` before completion.

Never add secrets, hidden network calls, unapproved permissions, or speculative dependencies. Report files changed, commands run, results, and known limitations.
